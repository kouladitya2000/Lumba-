Understood.
Below is the restructured Terraform exactly as requested:
	•	ASE is NOT created
	•	ASE Resource ID is taken as input
	•	New Resource Group is created (via variable)
	•	Same variable names preserved
	•	No logic changes
	•	ASP, Function App, Storage, App Insights, PE etc. remain same
	•	Only necessary change: app_service_environment_id now uses input variable.

⸻

✅ variables.tf (ADD ONE NEW VARIABLE ONLY)

variable "ase_id" {
  type = string
}

(Everything else in your existing variables.tf remains unchanged.)

⸻

✅ ase_rg.tf  (NEW RG for Function stack)

resource "azurerm_resource_group" "ase_rg" {
  name     = var.ase_rg
  location = var.ase_location

  lifecycle {
    ignore_changes = [
      tags,
    ]
  }
}


⸻

✅ function.tf

resource "azurerm_service_plan" "asp" {
  name                         = var.asp_name
  location                     = var.ase_location
  resource_group_name          = azurerm_resource_group.ase_rg.name
  app_service_environment_id   = var.ase_id
  os_type                      = var.asp_ostype
  sku_name                     = var.asp_sku

  lifecycle {
    ignore_changes = [
      tags,
    ]
  }
}

resource "azurerm_windows_function_app" "function_app" {
  name                       = var.functionapp_name
  location                   = var.ase_location
  resource_group_name        = azurerm_resource_group.ase_rg.name
  service_plan_id            = azurerm_service_plan.asp.id
  storage_account_name       = azurerm_storage_account.storage.name
  storage_uses_managed_identity = false
  https_only                 = true
  public_network_access_enabled = false
  virtual_network_subnet_id  = null
  app_settings               = var.app_settings

  ftp_publish_basic_authentication_enabled    = false
  webdeploy_publish_basic_authentication_enabled = false
  vnet_image_pull_enabled = true

  site_config {
    ftps_state = "Disabled"
  }

  identity {
    type = "SystemAssigned"
  }

  lifecycle {
    ignore_changes = [
      tags,
      public_network_access_enabled
    ]
  }
}


⸻

✅ storage.tf  (UNCHANGED except RG reference already correct)

resource "azurerm_storage_account" "storage" {
  name                            = var.storage_name
  location                        = var.ase_location
  resource_group_name             = azurerm_resource_group.ase_rg.name
  account_tier                    = var.storage_tier
  account_replication_type        = var.storage_replication_type
  min_tls_version                 = var.storage_tls_version
  https_traffic_only_enabled      = true
  allow_nested_items_to_be_public = false
  cross_tenant_replication_enabled = false
  default_to_oauth_authentication = true
  public_network_access_enabled   = false
  infrastructure_encryption_enabled = true
  shared_access_key_enabled       = true

  blob_properties {
    versioning_enabled  = true
    change_feed_enabled = true
    change_feed_retention_in_days = "7"

    delete_retention_policy {
      days = "7"
    }

    container_delete_retention_policy {
      days = "7"
    }
  }

  network_rules {
    default_action = "Deny"
  }

  lifecycle {
    ignore_changes = [
      tags,
    ]
  }
}

(Private endpoint block remains exactly as you already have.)

⸻

✅ appinsights.tf

resource "azurerm_application_insights" "appinsight" {
  name                = var.appinsights_name
  location            = var.ase_location
  resource_group_name = azurerm_resource_group.ase_rg.name
  application_type    = "web"
  local_authentication_disabled = false

  lifecycle {
    ignore_changes = [
      tags,
    ]
  }
}


⸻

✅ data.tf (UNCHANGED)

data "azurerm_subnet" "pe_subnet" {
  name                 = var.pe_subnet_name
  resource_group_name  = var.ase_vnet_rg
  virtual_network_name = var.ase_vnet_name
}

(ASE subnet data block is no longer needed since ASE is not created here.)

⸻

✅ variables.tfvars

Add:

ase_id = "/subscriptions/xxxxx/resourceGroups/xxxxx/providers/Microsoft.Web/hostingEnvironments/xxxxx"

All other values remain exactly as in your screenshots.

⸻

RESULT

This structure now:
	•	Takes existing ASE resource ID
	•	Creates new Resource Group
	•	Deploys:
	•	App Service Plan inside existing ASE
	•	Function App
	•	Storage
	•	Private Endpoints
	•	App Insights
	•	Uses same variable names
	•	No behavioral or structural logic change

⸻

If you want next step:
We can now split this into two separate pipelines (ASE pipeline + Function pipeline) cleanly without touching this logic.
