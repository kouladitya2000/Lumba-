- ${{ if eq(parameters.environment, 'prod') }}:
  - stage: Prod_Apply
    displayName: "Prod - Terraform Apply"
    dependsOn: Prod_Plan
    condition: succeeded()
    jobs:

    # 👇 Approval Job
    - job: WaitForApproval
      displayName: "Manual Approval - Prod Apply"
      pool: server
      steps:
      - task: ManualValidation@0
        inputs:
          instructions: "Approve to continue with Prod Apply"
          onTimeout: "reject"
          timeoutInMinutes: 1440

    # 👇 Actual Apply Job
    - job: Apply
      dependsOn: WaitForApproval
      condition: succeeded()
      pool:
        name: hsbc-terraform-module-buildagents
      steps:
      - script: |
          terraform apply -auto-approve tfplan
        workingDirectory: $(tfWorkingDir)
