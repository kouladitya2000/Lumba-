foreach ($adGroup in $adGroups) {
    if (
        (
            $adGroup.EndsWith("-vaultreader") -or
            $adGroup.EndsWith("-reader")
        ) -and
        ($adGroup -notlike "*hsbc-spnkv-cloud-*")
    ) {
        $adGroupNames += $adGroup
    }
}
