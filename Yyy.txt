if($missingFiles.Count -gt 0)
{
    $functionName = "KCIReporting"   # since both map to same function
    $url = "https://$functionAppName.azurewebsites.net/admin/functions/$functionName"

    Invoke-WebRequest -Uri $url -Headers $header -Method Post -Body $bodyJson

    Write-Host "Triggered function once for missing files"
}
else
{
    Write-Host "All files present"
}
