param($Timer)

$storageAccountName = "autopowershellfaeunnp01"
$resourceGroupName  = "YOUR-RG"
$containerName      = "waf"

$ctx = (Get-AzStorageAccount -ResourceGroupName $resourceGroupName -Name $storageAccountName).Context

$summaryFiles = Get-AzStorageBlob -Container $containerName -Context $ctx |
Where-Object { $_.Name -like "*_summary.html" }

$tenantNav = @()

foreach ($file in $summaryFiles) {

    $tenant = $file.Name -replace "_summary.html"

    $tenantNav += [PSCustomObject]@{
        label       = $tenant
        tenantid    = $tenant
        summary     = "$tenant`_summary.html"
        summaryCSV  = "$tenant`_summary.csv"
        detailsCSV  = "$tenant`_details.csv"
        detailsHtml = "$tenant`_details.html"
    }
}

$tenantJson = ($tenantNav | ConvertTo-Json -Compress -Depth 5)

$outDir = $env:TEMP
$indexPath = Join-Path $outDir "index.html"

$indexHtml = @"
<html>
<head>
<meta charset='utf-8'>
<title>AppGw WAF Compare - All Tenants</title>
<style>
body{font-family:Arial;font-size:12px}
.bar{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-bottom:10px}
button{padding:6px 10px}
iframe{width:100%;height:82vh;border:1px solid #ccc}
</style>
</head>

<body>

<h2>All Tenants - Subscription Summary</h2>

<div class="bar">
<button id="prevBtn" onclick="prevTenant()">Previous</button>
<button id="nextBtn" onclick="nextTenant()">Next</button>

<span id="pos"></span>
<span id="tenantLabel"></span>

<a id="openSummary" href="#" target="_blank">Open summary</a>
<a id="openDetailsHtml" href="#" target="_blank">Open details (HTML)</a>
<a id="openSummaryCsv" href="#" target="_blank">Summary CSV</a>
<a id="openDetailsCsv" href="#" target="_blank">Details CSV</a>

</div>

<iframe id="frame" src="about:blank"></iframe>

<script>

const tenants = $tenantJson
let idx = 0

function render(){

 if(!tenants || tenants.length===0){
  document.getElementById('tenantLabel').innerText='No tenant outputs were generated'
  return
 }

 if(idx<0) idx=0
 if(idx>=tenants.length) idx=tenants.length-1

 const t = tenants[idx]

 document.getElementById('tenantLabel').innerText = t.label
 document.getElementById('pos').innerText = (idx+1)+' of '+tenants.length

 document.getElementById('frame').src = t.summary

 document.getElementById('openSummary').href = t.summary
 document.getElementById('openDetailsHtml').href = t.detailsHtml
 document.getElementById('openSummaryCsv').href = t.summaryCSV
 document.getElementById('openDetailsCsv').href = t.detailsCSV

 document.getElementById('prevBtn').disabled = (idx===0)
 document.getElementById('nextBtn').disabled = (idx===tenants.length-1)

}

function prevTenant(){ idx--; render(); }
function nextTenant(){ idx++; render(); }

render()

</script>

</body>
</html>
"@

$indexHtml | Out-File $indexPath -Encoding utf8

Set-AzStorageBlobContent `
-File $indexPath `
-Container $containerName `
-Blob "index.html" `
-Context $ctx `
-Force
