Good. Then we’ll keep your original design:

You want:
	•	blob
	•	queue
	•	table
	•	file

All 4 services.
No simplification. No reduction.

We’ll just fix it properly without changing your structure.

⸻

✅ FINAL — EXACTLY WHAT YOU NEED

⸻

1️⃣ variables.tf

Keep this:

variable "storage_services" {
  type = map(object({
    name = string
  }))
}

Nothing more. Nothing less.

⸻

2️⃣ terraform.tfvars

Keep all 4 exactly like this:

storage_services = {
  blob = {
    name = "blob"
  }
  queue = {
    name = "queue"
  }
  table = {
    name = "table"
  }
  file = {
    name = "file"
  }
}

No DNS IDs here.

⸻

3️⃣ data.tf

You already have subnet data blocks.

Add ONLY this:

data "azurerm_virtual_network" "ase_vnet" {
  name                = var.ase_vnet_name
  resource_group_name = var.ase_vnet_rg
}

Add it at the bottom of the same file.

Done.

⸻

4️⃣ Create NEW file → private-dns.tf

Create file in same folder:

private-dns.tf

Paste FULL content below:

resource "azurerm_private_dns_zone" "blob" {
  name                = "privatelink.blob.core.windows.net"
  resource_group_name = var.ase_vnet_rg
}

resource "azurerm_private_dns_zone" "queue" {
  name                = "privatelink.queue.core.windows.net"
  resource_group_name = var.ase_vnet_rg
}

resource "azurerm_private_dns_zone" "table" {
  name                = "privatelink.table.core.windows.net"
  resource_group_name = var.ase_vnet_rg
}

resource "azurerm_private_dns_zone" "file" {
  name                = "privatelink.file.core.windows.net"
  resource_group_name = var.ase_vnet_rg
}

resource "azurerm_private_dns_zone_virtual_network_link" "blob_link" {
  name                  = "blob-link"
  resource_group_name   = var.ase_vnet_rg
  private_dns_zone_name = azurerm_private_dns_zone.blob.name
  virtual_network_id    = data.azurerm_virtual_network.ase_vnet.id
}

resource "azurerm_private_dns_zone_virtual_network_link" "queue_link" {
  name                  = "queue-link"
  resource_group_name   = var.ase_vnet_rg
  private_dns_zone_name = azurerm_private_dns_zone.queue.name
  virtual_network_id    = data.azurerm_virtual_network.ase_vnet.id
}

resource "azurerm_private_dns_zone_virtual_network_link" "table_link" {
  name                  = "table-link"
  resource_group_name   = var.ase_vnet_rg
  private_dns_zone_name = azurerm_private_dns_zone.table.name
  virtual_network_id    = data.azurerm_virtual_network.ase_vnet.id
}

resource "azurerm_private_dns_zone_virtual_network_link" "file_link" {
  name                  = "file-link"
  resource_group_name   = var.ase_vnet_rg
  private_dns_zone_name = azurerm_private_dns_zone.file.name
  virtual_network_id    = data.azurerm_virtual_network.ase_vnet.id
}

Nothing else in this file.

⸻

5️⃣ storage.tf — FINAL FIX

Inside your azurerm_private_endpoint block, modify ONLY the DNS part.

Find this:

private_dns_zone_ids = [each.value.private_dns_zone_id]

DELETE that line completely.

Replace your private_dns_zone_group block with this:

private_dns_zone_group {
  name = "default"

  private_dns_zone_ids = [
    lookup({
      blob  = azurerm_private_dns_zone.blob.id
      queue = azurerm_private_dns_zone.queue.id
      table = azurerm_private_dns_zone.table.id
      file  = azurerm_private_dns_zone.file.id
    }, each.key)
  ]
}

Do not change anything else.

Keep your for_each.
Keep your structure.
Keep your naming.

⸻

📁 Final File Layout

You will have:

main.tf
variables.tf
terraform.tfvars
storage.tf
data.tf
private-dns.tf

That’s it.

⸻

🚀 Then Run

terraform init
terraform plan


⸻

This keeps your original architecture fully intact.
No shortcuts.
No removals.
All 4 services.
Enterprise compliant.

If error comes, paste the full error block — not summary — and we fix that specific issue.
